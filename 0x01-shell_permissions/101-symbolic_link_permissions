#!/bin/bash

# Define the target file and the desired owner and group
target_file="_hello"
owner="vincent"
group="staff"

# Change the owner and group owner of the symbolic link
sudo chown -h "$owner:$group" "$target_file"
